in_dir: 'gs://jcosme/clean_data/genomics/data_splits' # absolute path to output destination of parquet or csv file

out_dir: 'gs://jcosme/clean_data/genomics/data_splits_inverse' # absolute path to output destination

dsplit_file: '/home/jovyan/work/projects/GPU_gene_ETL/configs/make_data_splits_config.yaml'

inverse_col: 'seq'

CUDA_VISIBLE_DEVICES: '0'
do_cuda_vis_dev: false # set to true if you only want to use 1 GPU (set by CUDA_VISIBLE_DEVICES)
partition_size: '100M'